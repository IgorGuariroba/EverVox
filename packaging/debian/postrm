#!/bin/sh
# Pós-remoção do pacote evervox: recarrega as regras udev sem a regra do
# uinput que acabou de sair.
set -e

case "$1" in
    remove|purge)
        if command -v udevadm >/dev/null 2>&1; then
            udevadm control --reload-rules || true
        fi
        ;;
esac

exit 0
